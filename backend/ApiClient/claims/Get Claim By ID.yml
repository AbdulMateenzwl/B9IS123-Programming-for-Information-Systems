info:
  name: Get Claim By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/claims/{{claimId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete Claim (Draft only)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/claims/{{claimId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get All Claims
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/claims"
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.body && res.body.length > 0) {
          bru.setEnvVar("claimId", res.body[0]._id);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get User By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/{{userId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Users (Filter by Role)
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/users?role=manager"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

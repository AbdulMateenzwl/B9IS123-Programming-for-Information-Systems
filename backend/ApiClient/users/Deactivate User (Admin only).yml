info:
  name: Deactivate User (Admin only)
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/users/{{userId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Budgets (Filter by Year)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/budgets?fiscalYear=2025"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

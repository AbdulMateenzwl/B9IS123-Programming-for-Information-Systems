info:
  name: Get Department By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/departments/{{departmentId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Pending Approvals (Manager+)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/workflow/pending"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
